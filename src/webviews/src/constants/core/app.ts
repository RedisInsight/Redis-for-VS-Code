import * as l10n from '@vscode/l10n'

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export const DEFAULT_ERROR_MESSAGE = l10n.t('Something was wrong!')
export const APPLICATION_NAME = 'Redis for VS Code'
